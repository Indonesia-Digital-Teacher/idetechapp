# Log Perubahan — 7 Juli 2026

**Waktu:** 10:55 – 11:06 WIB  
**Developer:** Feri Lee × Antigravity AI

---

## 1. Pembatasan Kuota Generate AI untuk Guru

### Backend
- **`src/server/lib/settings.ts`** — Menambahkan tipe `AiGenerationQuotaConfig`, nilai default (1x/hari), dan fungsi `getAiGenerationQuotaConfig()` mengikuti pola `getChatQuotaConfig` yang sudah ada.
- **`src/server/routes/api.ts`** — Memperbarui endpoint `/api/teacher/generate-ai` untuk:
  - Mengambil limit dari konfigurasi sistem (`system_settings`)
  - Mendukung *override* per email guru
  - Reset otomatis kuota setiap pukul **06:00 WIB** berdasarkan evaluasi `lastResetAt`
  - Mengembalikan HTTP 429 jika limit terlampaui

### Admin Dashboard
- **`src/client/main.tsx`** — Menambahkan panel konfigurasi **"Kuota Generate AI Guru"** di halaman Pengaturan Sistem Admin, dengan field:
  - `defaultLimit` — limit default harian untuk semua guru
  - `overrides` — override per guru dalam format `email=angka`

---

## 2. Redesain Halaman Orang Tua

### Visual Overhaul
- **`src/client/ParentFriendlyDashboard.tsx`** — Desain ulang penuh dari *light mode* ke **dark mode** (Zinc palette: `bg-[#09090b]`), seragam dengan halaman Siswa dan Guru.
- Semua kartu diganti ke `professional-card` dengan border `border-white/10` dan efek glassmorphism.
- **Bottom navigation bar** diperbarui dengan efek glow/neon aktif dan backdrop blur.

### Fitur Baru
- **Horizontal Child Selector** — Avatar scrollable horizontal di bawah header untuk berpindah antar anak dengan cepat.
- **HUD Progres Belajar** — Grafik lingkaran bergaya *sci-fi / gamification* dengan drop-shadow.
- **Timeline Aktivitas** — Riwayat belajar bergaya *quest tracker* vertikal.

---

## 3. Fitur Kotak Konsultasi Orang Tua ↔ Guru

### Database
- **`src/server/db/schema.ts`** — Menambahkan dua tabel baru:
  - `consultation_threads` — menyimpan utas konsultasi (topik, status: `open`/`closed`, referensi siswa/orang tua/guru)
  - `consultation_messages` — menyimpan isi pesan per utas
- **`src/server/db/migrations/0004_consultations.sql`** — File DDL migrasi manual.
- Migrasi dieksekusi langsung ke database MySQL menggunakan `bun -e`.

### API Endpoints Baru (`src/server/routes/api.ts`)

| Method | Endpoint | Aktor | Fungsi |
|--------|----------|-------|--------|
| GET | `/api/parent/teachers?studentId=` | Orang Tua | Daftar guru pengajar anak |
| GET | `/api/parent/consultations` | Orang Tua | Daftar semua utas konsultasi |
| POST | `/api/parent/consultations` | Orang Tua | Buat utas konsultasi baru |
| GET | `/api/parent/consultations/:id` | Orang Tua | Detail utas + riwayat pesan |
| POST | `/api/parent/consultations/:id/reply` | Orang Tua | Kirim balasan |
| GET | `/api/teacher/consultations` | Guru | Kotak masuk konsultasi |
| GET | `/api/teacher/consultations/:id` | Guru | Detail utas + riwayat pesan |
| POST | `/api/teacher/consultations/:id/reply` | Guru | Kirim balasan |
| POST | `/api/teacher/consultations/:id/close` | Guru | Tutup utas konsultasi |

### UI — Parent Dashboard (`src/client/ParentFriendlyDashboard.tsx`)
- Tab **💬 Pesan** baru di navigasi bawah (total 4 tab: Anak, Pesan, Laporan, Profil).
- **Daftar Utas** — list konsultasi dengan topik, nama guru, dan badge status.
- **Ruang Obrolan** — tampilan chat medsos: pesan sendiri biru di kanan, guru abu di kiri, timestamp per pesan.
- **Input Balasan** — field *pill rounded* + tombol kirim biru dengan shadow glow.
- **Modal Konsultasi Baru** — form pilih guru (filter berdasarkan kelas aktif anak), topik, dan isi pesan pertama.

### UI — Teacher Dashboard (`src/client/main.tsx` + `src/client/components/TeacherConsultationModal.tsx`)
- Ikon **💬 baru di header kiri** Dashboard Guru sebagai shortcut ke kotak konsultasi.
- Komponen baru `TeacherConsultationModal`:
  - Daftar pesan masuk dari orang tua dengan status open/closed
  - Ruang obrolan + input balas
  - Tombol "Tutup" utas (mengubah status ke `closed`)

---

## Catatan Teknis

- Semua query Drizzle ORM yang menggunakan `c.req.param("id")` (type `string | undefined`) dibungkus dengan `String()` untuk menghindari error overload TypeScript `TS2769`.
- Error TypeScript pre-existing (`coins`, `checkInStreak` di `AuthUser`) tidak termasuk dalam scope perubahan hari ini.
- Dev server berjalan normal di `localhost:2016`.
